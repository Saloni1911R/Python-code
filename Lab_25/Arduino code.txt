int x;

void setup() {
  Serial.begin(115200);
  Serial.setTimeout(50);   // higher timeout for stable read
}

void loop() {
  if (Serial.available()) {
    x = Serial.readStringUntil('\n').toInt();  // safer reading
    Serial.println(x + 1);
  }
}
